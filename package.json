{
  "name": "go_web_echo_app",
  "version": "1.0.0",
  "description": "a node client for the postgres db container",
  "main": "client.js",
  "scripts": {
    "ci": "npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.11.3"
  }
}
