name: go_web_echo_app with name spaces

on:
  push:
    branches:
      - stravid87/testing_t1
      
jobs:
  Unit-Tests:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: "Test Utils"
        run: echo "This is were tests will happen"
